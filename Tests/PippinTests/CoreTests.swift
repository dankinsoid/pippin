// @ai-generated(solo)
import Testing

// clj_debug_live_objects is process-wide, so every suite that allocates nests here to run serially, and
// booted before its first baseline.
@Suite(.serialized, .booted) struct CoreTests {}
