;; Forms the compiler refuses under --closed (CLJ_CORPUS_CLOSED=1), each with a :note saying why that is by design;
;; an entry without :line covers the whole file.
[{:file "test/clojure/core_test/eval.cljc" :note "eval needs the interpreter: refused in a closed unit by design (design §6, NOTES \"Compiler\")"}]
